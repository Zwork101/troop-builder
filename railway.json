{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacksPlan": {
            "providers": ["python"],
            "phases": {
                "setup": {
                    "nixPkgs": ["...", "ruby", "awscli2"]
                },
                "install": {
                    "cmds": ["bundle install", "gem uninstall --ignore-dependencies ffi -a", "gem install ffi --platform=ruby"]
                }
            }
        }
    },
    "deploy": {
        "startCommand": "python main.py"
    }
}